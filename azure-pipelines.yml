trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

variables:
  GITHUB_TOKEN: $(Token)  # salve isso como variável secreta
  GITHUB_USER: NichollasLima
  GITHUB_REPO: AllThingsServeTheTower

steps:
  - checkout: self

  - script: |
      git config --global user.name "Azure Mirror"
      git config --global user.email "azure@mirror.dev"
      git branch
      git remote add github https://$(GITHUB_USER):$(Token)@github.com/$(GITHUB_USER)/$(GITHUB_REPO).git
      git push github HEAD:main --force
    displayName: 'Mirror to GitHub'
