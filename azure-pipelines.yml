trigger:
  branches:
    include:
      - Main

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self

  - script: |
      git remote add github https://azure.devops:$(Token)@github.com:NichollasLima/AllThingsServeTheTower.git
      git push github main --force
    displayName: 'Mirror to GitHub'